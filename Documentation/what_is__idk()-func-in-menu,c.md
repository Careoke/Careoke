## What is function `idk()` in menu.c?

### Here is the [Ref](../src/menu.c#L42)

- It is half joke and half "works so no touch!"
  - while i was building on [this](../src/menu.c#L467) i added a random empty function for just place holder but after a while i dind't do anything and it was working fine so i din't touched

- if i use `continue` will it works fine?
  - yeah but it' a good joke XD
